<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
	<div class="container-fluid">
		<a class="navbar-brand" href="{{ path('app_home') }}"><img src="{{ asset('logo-wbg.png') }}" alt="" width="150px"></a>
		<button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarScroll" aria-controls="navbarScroll" aria-expanded="false" aria-label="Toggle navigation">
			<span class="navbar-toggler-icon"></span>
		</button>
		<div class="collapse navbar-collapse justify-content-end" id="navbarScroll">
			<ul class="navbar-nav navbar-nav-scroll" style="--bs-scroll-height: fit-content;">
				<li class="nav-item">
					<a class="nav-link text-green fw-bold" href="#">
						<i class="fas fa-gamepad"></i>
						Gaming'Room</a>
				</li>
				<li class="nav-item">
					<a class="nav-link text-purple fw-bold" href="#">
						<i class="fas fa-film"></i>
						Home Cinema</a>
				</li>
				<li class="nav-item fw-bold">
					<a class="nav-link text-warning" href="#">
						<i class="fas fa-holly-berry"></i>
						Matériel de fête</a>
				</li>

				{% if app.user %}
					<div class="dropdown" id="container-user-btnActions">
						<button class="btn btn-outline-secondary dropdown-toggle text-white dropdown-toggle" type="button" data-bs-toggle="dropdown" aria-expanded="false">
							<i class="fas fa-user-circle fa-lg"></i>
						</button>
						<ul class="dropdown-menu btn-bg-style mt-dropdownmenu p-0">
							<li>
								<a class="dropdown-item" href="{{ path('app_backoffice_home') }}">Mes informations</a>
							</li>
							{# Hides the menu if not Admin
							https://symfonycasts.com/screencast/symfony2-ep2/twig-security-is-authenticated #}
							{% if is_granted('ROLE_ADMIN') %}
							<li class="">
								<a class="dropdown-item" href="{{ path('app_backoffice_home') }}">Backoffice</a>
							</li>
							{% endif %}
						</ul>
					</div>
				{% endif %}

				<div class="dropdown-center">
					<a class="btn {{ app.user ? 'dropdown-center btn-outline-danger mx-0 my-1 mx-md-0 my-md-1 mx-lg-1 my-lg-0' : 'btn-outline-info' }}" href="{{ app.user ? path('app_logout') : path('app_login') }}">{{ app.user ? '<i class="far fa-times-circle"></i> Se déconnecter' : '<i class="fas fa-sign-in-alt"></i> Se connecter' }}
					</a>
				</div>
			</ul>
		</div>
	</div>
</nav>
