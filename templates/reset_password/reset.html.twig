{% extends 'base.html.twig' %}

{% block title %}Réinitialise ton mot de passe - {{ parent() }}{% endblock %}

{% block body %}
    <h1>Réinitialise ton mot de passe </h1>

    {{ form_start(resetForm) }}
        {{ form_row(resetForm.plainPassword) }}
        <button class="btn btn-primary">Réinitialise le mot de passe</button>
    {{ form_end(resetForm) }}
{% endblock %}
